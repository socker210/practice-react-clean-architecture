class TodoRepository {
	// 전체 todo list 가져오기
	getAllTodo() {}

	// id의 todo 가져오기
  getTodo(id) {}

	// todo 저장
  setTodo(todo) {}

	// id의 todo 제거
	removeTodo(id) {}
	
	// 전체 todo list 제거
	removeAllTodo()
}

export default TodoRepository